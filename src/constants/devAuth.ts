/**
 * Preview / development test login.
 *
 * Create this user once in Supabase Dashboard → Authentication → Users → Add user:
 * - Email: same as DEV_TEST_EMAIL
 * - Password: same as DEV_TEST_PASSWORD
 *
 * Supabase default minimum password length is 6, so the password is six sixes, not four.
 */
export const DEV_TEST_EMAIL = "test@oestra.preview";
export const DEV_TEST_PASSWORD = "666666";
